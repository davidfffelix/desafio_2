import 'package:flutter/material.dart';

// Definir os atributos para classe ListFilterItem
class ListFilterItem {
  final Widget icon;
  final String? label;

  ListFilterItem({
    required this.icon,
    this.label,
  });
}
